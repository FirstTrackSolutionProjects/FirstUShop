import React from 'react';
import MerchantWishlist from './Merchant/Wishlist';

// Simple wrapper so /wishlist route shows the wishlist UI used in merchant/profile
const Wishlist = () => {
  return <MerchantWishlist />;
};

export default Wishlist;
