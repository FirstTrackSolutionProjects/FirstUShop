import React from 'react';
import AdminProfile from '../AdminProfile';

// SuperAdmin profile view (wraps existing AdminProfile for now)
const SuperProfile = () => <AdminProfile />;

export default SuperProfile;
